# analytics

Computes family activity statistics over a date window.

## Examples

```bash
skycli analytics --days 30
skycli analytics --days 7 --json
skycli --readonly analytics --days 7 --json
```

## Output

JSON mode returns computed summary data for the requested window.

## Safety

Read-only.
